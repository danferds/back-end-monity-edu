spring.application.name=monity-edu

# URL de conex�o com o banco de dados PostgreSQL
spring.datasource.url=${DB_URL}

# Usu�rio e senha do banco de dados
spring.datasource.username=${DB_USER}
spring.datasource.password=${DB_PASSWORD}

# Configura��es do Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Configura��es do JWT
jwt.secret=${JWT_SECRET}
jwt.expiration=${JWT_EXPIRATION}

# ApyHub para valida��o de dom�nio
apyhub.api.key=${APYHUB_API_KEY}

# Clearout  para valida��o de exist�ncia de email
email.verify.api.key=${EMAIL_VERIFY_API_KEY}

# Tamanho m�ximo por arquivo
spring.servlet.multipart.max-file-size=10MB

# Tamanho m�ximo da requisi��o total
spring.servlet.multipart.max-request-size=50MB